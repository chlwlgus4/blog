server:
  port: 9090

spring:
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
    devtools:
      livereload:
        enabled: true

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://110.8.197.253:9004/choi?characterEncoding=UTF-8&serverTimezone=UTC
    username: chlwl
    password: chlwl695


  jpa:
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false
    show-sql: true
    properties:
      hibernate.format_sql: true
    open-in-view: true

  jackson:
    serialization:
      fail-on-empty-beans: false